# Q1.3: System Status Report

**Difficulty:** medium | **XP:** 75

## Scenario

Display a 3-line system status report.

## Instructions

1. Use `input()` to get `app_name`, `version`, and `status` (all str).
2. Print exactly 3 lines: `[APP_NAME] v{version}`, `Status: {status}`, `---`
3. Each on a new line.

## Hints

1. Use multiple `print()` calls or `\n` in one string.
2. `print()` adds newlines by default.
3. F-strings keep formatting clean: `f"[{app_name}] v{version}"`

## Solution

See `solution_Q_1_3.py` for the completed solution.

---
*Auto-generated by PyZeroToHero*
