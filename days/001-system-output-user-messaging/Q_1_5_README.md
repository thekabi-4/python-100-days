# Q1.5: Progress Bar

**Difficulty:** hard | **XP:** 100

## Scenario

Display a text-based progress bar for long operations.

## Instructions

1. Use `input()` to get `current` (int), `total` (int), and optional `label` (str, default 'Loading').
2. Print: `[{filled}{empty}] {percent}% - {label}` where bar width=10.
3. Filled=`#`, empty=`.`, percent=`(current*100)//total`.

## Hints

1. `percent = (current * 100) // total`, `filled = (current * 10) // total`
2. Build bar: `"#" * filled + "." * (10 - filled)`
3. Combine with f-string and print.

## Solution

See `solution_Q_1_5.py` for the completed solution.

---
*Auto-generated by PyZeroToHero*
