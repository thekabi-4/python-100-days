# Q1.4: Log Entry Formatter

**Difficulty:** medium | **XP:** 75

## Scenario

Generate timestamped log entries for debugging.

## Instructions

1. Use `input()` to get `level` (str), `message` (str), and `line_num` (int).
2. Print: `[{level}] Line {line_num}: {message}`
3. Convert `line_num` to string via f-string or `str()`.

## Hints

1. Convert `line_num` to string via f-string or `str()`.
2. Exact format: `f"[{level}] Line {line_num}: {message}"`
3. No extra spaces allowed.

## Solution

See `solution_Q_1_4.py` for the completed solution.

---
*Auto-generated by PyZeroToHero*
