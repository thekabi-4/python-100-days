# Q2.2: Checkout Calculator

**Difficulty:** easy | **XP:** 50

## Scenario

Calculate e-commerce checkout total using tax.

## Instructions

1. Use `input()` to get `base_price` (float) and `tax_rate` (float).
2. Print `base_price + (base_price * tax_rate)`, rounded to 2 decimals.

## Hints

1. `total = base_price * (1 + tax_rate)`
2. Use `round(total, 2)`
3. Print result.

## Solution

See `solution_Q_2_2.py` for the completed solution.

---
*Auto-generated by PyZeroToHero*
