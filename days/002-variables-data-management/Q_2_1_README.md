# Q2.1: User Profile Creator

**Difficulty:** easy | **XP:** 50

## Scenario

Initialize a new user profile using defaults.

## Instructions

1. Use `input()` to get `username` (str), `age` (int), and `is_premium` (bool).
2. Create and print a dict: `{"name": username, "age": age, "premium": is_premium, "points": 100}`

## Hints

1. Use `input()` to get values.
2. Use `{}` for dictionaries.
3. Map inputs to keys directly.

## Solution

See `solution_Q_2_1.py` for the completed solution.

---
*Auto-generated by PyZeroToHero*
