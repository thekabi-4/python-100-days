# Q2.5: Currency Converter

**Difficulty:** hard | **XP:** 100

## Scenario

Convert prices between currencies via USD base rate.

## Instructions

1. Write a function `convert_currency` that takes `amount` (float), `from_currency` (str), `to_currency` (str), `rates` (dict).
2. Return `amount * rates[from] / rates[to]`, rounded to 2 decimals.
3. Same currency -> `round(amount, 2)`. Unknown currency -> `-1.0`.

## Hints

1. Check same currency first -> `round(amount, 2)`
2. Check `from` or `to` missing in `rates` -> `-1.0`
3. Calculate via base, `round(result, 2)`

## Solution

See `solution_Q_2_5.py` for the completed solution.

---
*Auto-generated by PyZeroToHero*
