# Q2.4: Inventory Manager

**Difficulty:** medium | **XP:** 75

## Scenario

Update inventory after purchase, check low-stock alert.

## Instructions

1. Use `input()` to get `current_stock` (int), `purchased` (int), `low_stock_threshold` (int).
2. Print `{"remaining": new_stock, "low_stock_alert": bool}`.
3. Stock never goes negative. Alert if `remaining <= threshold`.

## Hints

1. `remaining = max(0, current_stock - purchased)`
2. `alert = remaining <= low_stock_threshold`
3. Print dict.

## Solution

See `solution_Q_2_4.py` for the completed solution.

---
*Auto-generated by PyZeroToHero*
